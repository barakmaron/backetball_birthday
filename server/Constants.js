const DateStampFormat = "YYYY-MM-DD";

const USER_LOGIN_ERROR = "Email And password do not match";

const REFRESH_TOKEN_MAX_AGE = 24 * 60 * 60 * 1000;

const Constants = {
    DateStampFormat,
    USER_LOGIN_ERROR,
    REFRESH_TOKEN_MAX_AGE
};

export default Constants;